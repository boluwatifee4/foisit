# Server-side LLM provider configuration for dev assistant proxy
DEV_ASSISTANT_PROVIDER_URL=https://api.openai.com/v1/chat/completions
DEV_ASSISTANT_API_KEY=sk-REPLACE_ME

# Optional: override the proxy path for clients (if served from different origin)
# VITE_DEV_ASSISTANT_PROXY=/api/dev-assistant
